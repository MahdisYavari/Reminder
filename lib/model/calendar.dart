class Calendar {
  int id;
  String title;
  String description;
  String dateCreated;
  String timeSelected;
  String reminderState;

  Calendar(this.id, this.title, this.description, this.dateCreated, this.timeSelected, this.reminderState);
}
